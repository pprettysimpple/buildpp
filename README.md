### Build system for prosperity

0) install compiler that supports c++20. example will use global clang++
1) create `build.cpp` file
2) put `buildpp.h` near it
3) copy content of example at the end of `buildpp.h` file into your `build.cpp`
4) Follow comments at the example! Good luck!
