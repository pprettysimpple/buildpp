#include <gtest/gtest.h>

TEST(Example03Deps, BasicAssertions) {
    // Just a dummy test to make sure the build works
    EXPECT_EQ(1 + 1, 2);
}

// TEST(Example03Deps, BasicAssertionsFailed) {
//     // Just a dummy test to make sure the build works
//     EXPECT_EQ(1 + 1, 3);
// }
